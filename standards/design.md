When working on design or UI:

- Follow the project's existing design system or component library.
- Use design tokens (colors, spacing, typography) from the project's theme, not hardcoded values.
- Maintain visual consistency with existing screens before introducing new patterns.
- Consider responsive behavior across viewport sizes.
- Prefer the project's established spacing and layout system.
- Ensure sufficient color contrast for accessibility.
