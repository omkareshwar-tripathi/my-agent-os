When working on frontend code:

- Prefer semantic HTML elements over generic divs.
- Keep components small and focused on one responsibility.
- Separate data fetching from presentation when practical.
- Use established component patterns in the project before introducing new ones.
- Test user-visible behavior, not implementation details.
- Consider accessibility from the start: labels, focus management, keyboard navigation.
- Match the project's existing state management approach.
