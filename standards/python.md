When working in a Python project:

- Follow the project's existing code style. If none exists, follow PEP 8.
- Use type hints for function signatures and class attributes.
- Use the project's dependency management tool (pip, poetry, uv) consistently.
- Prefer standard library solutions before adding dependencies.
- Use context managers for resource management.
- Use virtual environments. Never install into the system Python.
