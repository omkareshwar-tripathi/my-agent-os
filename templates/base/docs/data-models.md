# Data Models

Document persistent models, schemas, entities, relationships, migrations, and validation rules.

## Models

For each model, include its purpose, fields, relationships, invariants, and storage location.

## Migration Notes

Record compatibility constraints and migration rules agents must preserve.
